const COLUMNS =7;
const ROWS = 6;
var gameboard = Array(COLUMNS).fill(Array(ROWS).fill(0))

const placeChip = function (col) {
  var chipPlaced = false;
  gameboard[col][0] = "poop";
  
} 

const displayChip = function (row, col) {
  $(".row"+ row + " .col" + col).html("X");
}

$(document).ready(function() {


})